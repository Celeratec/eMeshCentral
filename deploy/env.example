# =============================================================================
# eCortex Production Environment Configuration
# =============================================================================
# INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Replace all placeholder values with actual secrets
# 3. NEVER commit .env to version control
# =============================================================================

# =============================================================================
# HOSTNAME & TLS
# =============================================================================
# Your eCortex server FQDN (must match DNS record)
MESHCENTRAL_HOSTNAME=mesh.cortalis.com

# Email for Let's Encrypt certificate notifications
ACME_EMAIL=admin@cortalis.com

# Timezone
TZ=America/Chicago

# =============================================================================
# MONGODB CREDENTIALS
# =============================================================================
# Generate strong passwords: openssl rand -base64 32
MONGO_ROOT_USER=meshcentral_admin
MONGO_ROOT_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD

# Application database credentials (used by eCortex)
MONGO_APP_USER=meshcentral
MONGO_APP_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD

# =============================================================================
# eCORTEX SECURITY
# =============================================================================
# Session key - MUST be unique and random
# Generate with: openssl rand -base64 64
MESHCENTRAL_SESSION_KEY=CHANGE_ME_GENERATE_64_CHAR_RANDOM_STRING

# Database encryption key (for encrypting sensitive data at rest)
# Generate with: openssl rand -base64 32
MESHCENTRAL_DB_ENCRYPT_KEY=CHANGE_ME_GENERATE_32_CHAR_RANDOM_STRING

# =============================================================================
# TRAEFIK DASHBOARD (optional)
# =============================================================================
# Generate with: htpasswd -nb admin YOUR_PASSWORD | sed -e s/\\$/\\$\\$/g
# Example: admin:$$apr1$$xyz... (note: $ must be doubled for docker-compose)
TRAEFIK_DASHBOARD_AUTH=admin:$$apr1$$CHANGE_ME

# =============================================================================
# AGENT DEPLOYMENT (DO NOT COMMIT REAL VALUES)
# =============================================================================
# These are EXAMPLES only - actual tokens are generated server-side
# and injected via NinjaOne secure variables
#
# MESH_INVITE_TOKEN=<generated-server-side>
# MESH_GROUP_ID=<device-group-id>
